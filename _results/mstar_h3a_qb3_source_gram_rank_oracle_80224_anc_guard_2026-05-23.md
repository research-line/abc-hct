# H3a Q_B-3 Source-Gram Rank Oracle

Level: `80224`
Status: `blocked_by_dense_guard`
Mode: `dense`

## Guard

Use a matrix-free Source-only Gram-rank oracle for this dimension.

Quotient dimension `10568` exceeds guard
`256`.

The large-level target is only the Source-only block
`rank(A)=10567`.
